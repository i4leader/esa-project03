# CodeReview Pro - 环境变量配置

# ============================================
# 使用模式说明
# ============================================
# 模式 1（推荐）：前端直连模式
#   - 用户在 UI 中输入阿里云 API Key
#   - 前端直接调用阿里云通义千问 API
#   - 无需配置环境变量
#   - 适合个人开发者和小团队
#
# 模式 2：后端服务模式（企业级）
#   - 需要部署 ESA Functions
#   - 统一管理 API Key
#   - 支持缓存和速率限制
#   - 需要配置以下环境变量

# ============================================
# 阿里云通义千问 API 配置（仅后端服务模式需要）
# ============================================

# 获取地址: https://dashscope.console.aliyun.com/
# VITE_ALIYUN_API_KEY=sk-your-aliyun-key-here
# VITE_ALIYUN_API_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
# VITE_ALIYUN_MODEL=qwen-turbo

# ============================================
# ESA Functions 配置（生产环境，仅后端服务模式需要）
# ============================================

# ESA Function 端点
VITE_API_ENDPOINT=/api/review
# 生产环境示例:
# VITE_API_ENDPOINT=https://your-function.esa.aliyuncs.com/api/review

# ============================================
# 功能开关
# ============================================

# 是否使用 Mock 数据（开发环境推荐设为 true）
# 设为 false 时，如果用户提供了 API Key，将调用真实的阿里云 API
VITE_USE_MOCK_DATA=true

# 是否启用缓存（仅后端服务模式）
VITE_CACHE_ENABLED=true

# 缓存过期时间（秒，默认 24 小时）
VITE_CACHE_TTL=86400

# 是否启用速率限制（仅后端服务模式）
VITE_RATE_LIMIT_ENABLED=true

# 速率限制：每分钟最大请求数
VITE_RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# 开发环境配置
# ============================================

# 开发调试模式
# VITE_DEBUG=true

# API 超时时间（毫秒）
VITE_API_TIMEOUT=30000

# ============================================
# 应用配置
# ============================================

# 应用名称
VITE_APP_NAME=CodeReview Pro

# 应用版本
VITE_APP_VERSION=1.0.0

# 应用环境 (development | production)
VITE_APP_ENV=development

# ============================================
# 使用说明
# ============================================
# 前端直连模式（推荐）：
#   1. 无需配置此文件
#   2. 启动应用: npm run dev
#   3. 在 UI 中点击 "Setup API" 按钮
#   4. 输入您的阿里云 API Key
#   5. 开始使用
#
# 后端服务模式：
#   1. 复制此文件为 .env
#   2. 取消注释并填写 VITE_ALIYUN_API_KEY
#   3. 配置 ESA Functions（参考 BACKEND_SETUP.md）
#   4. 重启开发服务器: npm run dev
#
# 详细配置说明请参考 BACKEND_SETUP.md
